import HeroContent from './HeroContent/HeroContent';
import './hero.scss';

const Hero = () => {
  return (
    <div className="hero">
      <HeroContent />
    </div>
  );
};

export default Hero;